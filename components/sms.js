import React from 'react';

// Import React Native components

// Imprort Redux and store depencencies

export default function Message() {

  // grab the safe contact set to state via redux

  // grab phone number off safe contact

  // send the safe contact a message and a desired destination

  // message contact once current location === desired location

};